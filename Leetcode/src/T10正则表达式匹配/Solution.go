package main

import "fmt"

func isMatch(s string, p string) bool {
	var flag1, flag2 bool = false, false
	for i := 0; i < len(s) && i < len(p); i++ {

	}

	return flag1 && flag2
}

func main() {
	fmt.Printf("%v\n", 100)
}
